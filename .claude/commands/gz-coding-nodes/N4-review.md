# N4: 审查规范 (Lambda Focus)

## 检查清单
1. **冷启动优化**：检查是否引入了过大的包（如大型库的整个导入）。
2. **环境变量**：确保所有敏感信息都在 `.env` 中，且在 `packages/env` 中有类型定义。
3. **Better-Auth 兼容性**：检查认证中间件是否会造成死循环或高延迟。
4. **错误处理**：tRPC 必须抛出标准的 `TRPCError` 供前端捕获。

✓ 如果审查失败，回退到 N3 重修。
✓ 审查通过进入 N5，并识别当前 Cycle 编号，更新 WORKFLOW.md 中对应的 `Cycle{N}_N4` 为 completed。